// =============================================
// app.js — Bootstrap de la aplicación
// =============================================

document.addEventListener('DOMContentLoaded', () => {
  // Inicializar el router — determina vista inicial según sesión activa
  Router.init();
});
